// app/signup/layout.js
export const metadata = {
  title: "Sign Up - Expense Tracker",
  description: "Create a new account to start tracking your expenses",
};

export default function SignupLayout({ children }) {
  return <>{children}</>;
}