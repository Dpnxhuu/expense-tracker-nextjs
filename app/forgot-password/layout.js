export const metadata = {
  title: "Forgot Password — Expense Tracker",
  description: "Reset your Expense Tracker account password",
}

export default function ForgotPasswordLayout({ children }) {
  return children
}