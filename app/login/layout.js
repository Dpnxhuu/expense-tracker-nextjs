// app/login/layout.js
export const metadata = {
  title: "Login - Expense Tracker",
  description: "Login to your account to track your expenses",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}